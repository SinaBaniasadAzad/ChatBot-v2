openai>=1.40.0
pydantic>=2.6
PyYAML>=6.0
python-dotenv>=1.0
fastapi>=0.110
uvicorn[standard]>=0.29
pytest>=8.0

# گزارش‌گیری (داشبوردِ PNG و خروجیِ Excelِ خطاها)
matplotlib>=3.7
numpy>=1.24
openpyxl>=3.1

# اختیاری — برای خروجیِ PDF (هرکدام کافی است؛ خودکار انتخاب می‌شود):
#   weasyprint  → بدونِ مرورگر، پیشنهادی روی Kaggle
#   playwright  → بالاترین فیدلیتی (پس از نصب: playwright install chromium)
# weasyprint>=60
# playwright>=1.40
