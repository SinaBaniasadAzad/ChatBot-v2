# وابستگی‌های runtime — نسخه‌ها *دقیقاً* پین شده‌اند (بازتولیدپذیری بیلد).
# منبعِ نسخه‌ها: محیطِ توسعه‌ای که تست‌ها با آن سبز است (Python 3.12).
# ارتقا: نسخه را اینجا عوض کن → pytest → ایمیجِ جدید بساز. dev/گزارش‌گیری: requirements-dev.txt

# --- Web framework ---
fastapi==0.139.0
starlette==0.52.1
pydantic==2.13.4
pydantic_core==2.46.4
uvicorn==0.50.0          # عمداً بدونِ [standard]: در بارِ ~۴ درخواست/دقیقه، uvloop/httptools سودی ندارند
click==8.4.2
h11==0.16.0
anyio==4.14.1
sniffio==1.3.1
idna==3.18
annotated-types==0.7.0
typing_extensions==4.16.0
typing-inspection==0.4.2

# --- DeepSeek (OpenAI-compatible SDK) ---
openai==2.44.0
httpx==0.28.1
httpcore==1.0.9
certifi==2026.6.17
distro==1.9.0
jiter==0.16.0
tqdm==4.68.3

# --- Config / taxonomy ---
PyYAML==6.0.3
python-dotenv==1.2.2

# --- Retrieval index math (بارگذاری ایندکسِ npz؛ مدلِ embedding جداست: requirements-retrieval.txt) ---
numpy==2.5.0

# --- Windows-only (توسعهٔ محلی؛ در ایمیجِ لینوکسی نصب نمی‌شود) ---
colorama==0.4.6 ; sys_platform == "win32"
